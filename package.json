{
    "name": "codeceptjs-tests",
    "version": "0.1.0",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "codeceptjs": "^3.0.5",
        "puppeteer": "^7.1.0"
    }
}
